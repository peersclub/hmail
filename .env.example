# Copy to .env and fill in real values. .env is gitignored — never commit it.

# AI insights — provide at least one. Claude is preferred.
CLAUDE_API_KEY=
OPENAI_API_KEY=

# Google OAuth client ID (Google Cloud Console → APIs & Services → Credentials).
# Leave empty to run the app in demo mode with canned data.
GOOGLE_CLIENT_ID=
